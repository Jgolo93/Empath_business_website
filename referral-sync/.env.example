# Copy this file to .env for local development (never commit .env).
# In production, set these as Vercel Project env vars AND/OR GitHub Actions repo secrets
# (whichever mechanism actually runs this script — see note in vercel.json).

DATABASE_URL=

ZOHO_ORG_ID=
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_ACCOUNTS_URL=https://accounts.zoho.com
ZOHO_API_DOMAIN=https://desk.zoho.com

MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=
